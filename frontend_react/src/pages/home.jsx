import { Typography } from '@mui/joy';

import { Layout } from '../components';

const Home = () => {
    return (
        <Layout>
            <Typography level="display1">HELLO</Typography>
        </Layout>
    );
};

export default Home;
